# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
/node_modules/
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript cache
*.tsbuildinfo
tsconfig.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarnclean

# Next.js build output directories
/.next/
/out/

# Vercel deployment output
.vercel

# Production build directory (less common for Next.js on Vercel but good to have)
/build

# Local Environment Files
# IMPORTANT: Environment variables for Vercel should be set in the Vercel project settings.
# .env files (except .env.example) should typically not be committed.
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# If your .env file contains sensitive server-side information or you prefer managing ALL env vars
# (including NEXT_PUBLIC_ ones) via Vercel UI, uncomment the line below to ignore .env:
# .env
# Note: The current .env file in this project contains NEXT_PUBLIC_ Firebase keys.
# Committing these is generally acceptable as they are client-side, but for stricter control,
# manage them via Vercel UI and add .env to this gitignore.

# Editor directories and files (allowlist common ones)
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Keep .vscode/settings.json as it's already in the project and useful
!.vscode/settings.json

# Patch-package specific files
patches/

# Cypress e2e testing artifacts (if used)
# cypress/videos/
# cypress/screenshots/

# macOS specific
.DS_Store
